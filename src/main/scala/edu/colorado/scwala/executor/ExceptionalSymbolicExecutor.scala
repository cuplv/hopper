package edu.colorado.scwala.executor

/**
 * symbolic executor that soundly handles exceptional control flow
 */
class ExceptionalSymbolicExecutor(tf : TransferFunctions) extends DefaultSymbolicExecutor(tf) {

}
